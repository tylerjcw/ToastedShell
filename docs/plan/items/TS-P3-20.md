---
id: TS-P3-20
title: "A regex engine for the native target"
status: withdrawn
area: legacy
priority: 3
legacy: TS-P3-20
closed: 2026-09-25
---

> **Withdrawn 2026-09-25** under `TOAST-ARCH-01`: Tōast has no compiler ([COMPILER_DECOMMISSIONING_PLAN.md](../../COMPILER_DECOMMISSIONING_PLAN.md)). The native target is withdrawn with the compiler.

> Status as filed: Proposed — filed 2026-08-13

## Problem

A regex engine for the native target

## Acceptance

`strings.regex-literal` lowers through a runtime command invocation (Tier 2) onto .NET `Regex`. Native needs its own engine or a bound C library, and either way the dialect will differ from .NET — backreferences, lookbehind, named groups, and `RegexOptions` semantics. Decide deliberately whether ToastScript specifies its own regex dialect (see `TS-P3-16`) or accepts a documented divergence between targets. Silently differing regex behaviour between two backends would be among the worst possible outcomes.
