---
id: TOSH-0009
title: "TōSh can be built for Windows but not installed there"
status: partial
area: tosh
priority: 3
opened: 2026-08-26
---

## Problem

`build.tosh publish --rid win-x64` produces the right artifacts, and they run: measured
under Wine on 2026-08-26, the language, pipelines, classes, filesystem access, `$env` and
external process spawn all work, and `examples/collections.tosh` and
`examples/pattern_matching.tosh` run to completion. What does not exist is any way to
*install* the result.

Blocked on [`TOSH-0008`](TOSH-0008.md): until a cross-RID publish stops shipping the host's
SDK assemblies, an installer would package a build whose `--compile` aborts.

## What installing means, taken from what Linux actually does

`packaging/archlinux/PKGBUILD` is unglamorous, which is the useful part:

| Linux | Windows equivalent |
|---|---|
| five single-file executables into `/usr/bin`, renamed (`Tosh.Cli` → `tosh`) | the same five into `%LOCALAPPDATA%\Programs\Tosh\` |
| — | a user-level `PATH` entry, so no administrator is required |
| `tosh.install` adds `/usr/bin/tosh` to `/etc/shells` | no analogue; a Windows Terminal profile fragment is the nearest thing |
| `LICENSE` into `/usr/share/licenses` | beside the binaries |

No services, no daemons, no registry beyond `PATH`. The `/etc/shells` hook exists because
TōSh is a login shell, and that is the piece with no direct counterpart.

## Decision: extend `build.tosh`, do not write a separate installer

`build.tosh` does not install anything today — it publishes a validated bundle and hands it
to a platform packager (`makepkg` over the `PKGBUILD`). Windows needs a **second packager**,
not a second build system, so `packaging/windows/` sits beside `packaging/archlinux/` and
the install step dispatches on RID.

The alternative — a standalone installer project — would duplicate what `build.tosh`
already owns and has got right: CalVer bumping, `--targets` selection across the five
projects, `.tosh-publish-manifest` validation, the staging and rollback machinery, and the
cross-RID smoke-test skip that already exists for exactly this case.

## Decision: a zip and a PowerShell script first

It cross-builds from Linux with no new toolchain, is testable under Wine immediately, and
does what the Arch package does — place binaries, register a path.

`msitools` (`extra/msitools`, whose `wixl` authors MSI files on Linux) is the upgrade path
when Add/Remove Programs and a real uninstaller are wanted; it needs no Windows build
machine. `makensis` is not installed here and would have to be fetched.

## Progress — 2026-08-27

`packaging/windows/` holds `install.ps1` and `uninstall.ps1`; a `WindowsPackage` module in
`build.tosh` stages them into the promoted bundle with the licence and zips the result. It
runs where the Arch path runs — after promotion — so versioning, target selection, manifest
validation and the cross-RID smoke-test skip are all inherited rather than re-implemented.

`build.tosh publish --rid win-x64 --noInstall --noBump` produces
`artifacts/publish/win-x64/tosh-26.8.43.10-win-x64.zip`: five executables, both scripts and
`LICENSE`.

**Install is per-user by choice.** `%LOCALAPPDATA%\Programs\Tosh` and a user-scope `PATH`
entry, so nothing prompts for elevation. The Arch package writes to `/usr/bin` because a
system package manager is already root; nothing here is, and per-user keeps uninstall from
touching anyone else.

Three details worth recording:

- **A previous install is removed, not merged.** A self-contained bundle is a matched set of
  roughly 200 files, and copying a newer one over an older leaves whatever the new version
  stopped shipping — stale assemblies that still load.
- **`PATH` is read at user scope, never from `$env:PATH`.** The process copy is the machine
  and user values already joined; writing that back copies every machine entry into the
  user's own `PATH`, permanently.
- **Two executables are renamed on copy**, `Tosh.Lsp.exe` to `tosh-lsp.exe` and `Tosh.Mcp.exe`
  to `tosh-mcp.exe`, so what lands on `PATH` matches what the Arch package installs. The
  bundle keeps the publish names.

Both scripts parse under `pwsh`, and the copy, exclusion and rename behaviour is exercised
on Linux against a stand-in bundle. The `PATH` half cannot be: .NET refuses user-scope
environment writes off Windows.

## Not done

- Installed and run on real Windows. Wine gets the bundle built and the scripts parsed;
  neither proves an install.
- A Windows Terminal profile fragment, or a decision not to ship one.
- MSI via `msitools`, if Add/Remove Programs is wanted.

Found while doing this and filed separately: `build.tosh publish` reports a failure after
succeeding, on every RID and independent of this work — [`TOSH-0010`](TOSH-0010.md).

## Acceptance

- [x] `packaging/windows/` produces a zip of the five executables plus `LICENSE`
- [x] An `install.ps1` places them under `%LOCALAPPDATA%\Programs\Tosh` and adds that
      directory to the user `PATH` without requiring administrator
- [x] `build.tosh publish --rid win-x64` invokes it, dispatching on RID the way the Arch
      path is dispatched today
- [x] An uninstall path that removes both the files and the `PATH` entry
- [ ] Installed and run on real Windows, not only under Wine
- [ ] A Windows Terminal profile fragment, or a recorded decision not to ship one
