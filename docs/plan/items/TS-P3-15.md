---
id: TS-P3-15
title: "Define the `no_clr` language subset"
status: withdrawn
area: legacy
priority: 3
legacy: TS-P3-15
closed: 2026-09-25
---

> **Withdrawn 2026-09-25** under `TOAST-ARCH-01`: Tōast has no compiler ([COMPILER_DECOMMISSIONING_PLAN.md](../../COMPILER_DECOMMISSIONING_PLAN.md)). The native target and the tier model it depended on are withdrawn with the compiler.

> Status as filed: Proposed — filed 2026-08-13

## Problem

Define the `no_clr` language subset

## Acceptance

The gating design item for a native target. Specify exactly which constructs are available when the CLR is absent, and which are rejected at compile time with a diagnostic naming the profile. Modelled on Rust `no_std`: one language, a restricted profile, not a second language. Must state the rule that lets the checker prove no CLR value reaches native code — the hard part, because the language is largely dynamic and a conservative rejection of every un-inferred type would be unusably noisy. Blocks every other native item.
