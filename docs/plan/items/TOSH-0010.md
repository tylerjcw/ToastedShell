---
id: TOSH-0010
title: "`build.tosh publish` reports failure after succeeding, on every RID"
status: proposed
area: tosh
priority: 2
opened: 2026-08-27
---

## Problem

`build.tosh publish` does all of its work, prints its DONE banner, and then fails:

```
────────────[ DONE ]────────────
  ✓ Binary: …/artifacts/publish/linux-x64/single-file/tosh
  ✓ Version: 26.8.43.10
✖ error  tosh.runtime.return_type_conversion_failed
  Function 'Run' returned a value that could not be converted to 'Result'.
```

Exit code 1. Everything before the return is correct — the five projects publish, the bundle
is promoted, the version is written, and on the Arch path the packages are built and
installed. Only `Publish.Run`'s own return fails.

## Measured 2026-08-27

- **Both RIDs.** `--rid linux-x64` and `--rid win-x64` fail identically, so it is not
  cross-compilation.
- **Not a regression from current work.** The *installed* `/usr/bin/tosh`, dated 25 August
  and predating the session's changes, fails the same way on the same script.
- **The value is correctly shaped.** Instrumenting immediately before the return prints every
  field at its declared type — `Binary=System.String`, `Size=Tosh.Runtime.StorageSize`,
  `RID=System.String`, `Output=System.String`, `SelfContained=System.Boolean`,
  `Trimmed=System.Boolean`, `Duration=System.String`, `Version=System.String` — against a
  record declaring exactly those eight.
- **`type-of` says it *is* a `Result`.** The probe reports `built=Result`, and the value
  renders with all eight fields under their own names. A value the runtime calls a `Result`
  is then refused conversion to `Result`.

## What did not reproduce it

Worth recording so the next attempt does not repeat them:

- a record with a `StorageSize` field returned from an annotated function;
- the same, declared and returned inside a nested `module A { module B { … } }`;
- two modules each declaring a record named `Result`, with one returning its own.

All three work. `build.tosh` declares **three** records named `Result` — `Publish.Result`
(eight fields), `Benchmark.Result` (five) and `Spec.Result` (four) — which remains the
strongest suspect, but the two-module reduction above did not reproduce it, so something
further is involved.

## Why it matters more than the exit code

The publish path is how TōSh is released, and it currently ends every run by reporting a
failure that did not happen. That trains the reader to ignore its exit code, which is the
one signal that would report a failure that *did*.

## Acceptance

- [ ] `build.tosh publish` exits 0 when it succeeds
- [ ] A reduction that reproduces the conversion failure, small enough to live in the suite
- [ ] The cause named — whether it is the three same-named `Result` records, or something else
- [ ] A test covering it, in whichever of the language or the script the fault turns out to be
- [ ] A negative control
