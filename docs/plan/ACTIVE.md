# Active work

Items accepted and in play — open, in progress, under research, or partially done. This is the working list; if it is long, that is a signal.

## P2 — Parser, Binder, Diagnostics, and Surface Generation

| ID | Status | Problem | Required acceptance |
|---|---|---|---|
| `TS-P2-94` | Complete for named functions — fixed 2026-08-13; bound instance-method references remain, see intent | **`&` cannot reference a method or a module-qualified function.** The parser requires the token after `&` to be an adjacent bareword passing `IsValidCommandName`, and evaluation looks only in scope commands then the runtime registry. Measured: `&$p.Show` reports `tosh.parser.unexpected_background_operator`, `&Mod.Class.StaticFn` the same, and `&Point.Origin` reaches `tosh.runtime.unknown_function_reference`. Only a bare module-level function name works. Every C callback registration and every higher-order call over an object's own behaviour therefore needs a module-level dispatcher plus a hand-rolled token registry to recover the identity `&` could not carry. | `&Type.Method`, `&$obj.Method` and `&Module.func` each produce a callable bound to the right target, or report a diagnostic naming what was not found. A corpus covers a static method, an instance method, a module-qualified function, and the existing bare-name case as a control. **Cause: `&` reused the *command-name* rule, which forbids a dot.** So `&M.Exported` and `&C.Static` failed the guard, fell through, and were reported as a stray background operator — an error about a construct the reader had not written. The rule now has a function-reference variant that admits a dotted path whose every segment is a valid name; a dotted *command* name is still not a thing. **The rule existed in three places** — the argument parser, the pipeline-start check (`LooksLikeFunctionReferenceArgument`), and `LiteParser`'s structural pass — and relaxing two of them changed nothing, because the third rejected the text first. All three now share the predicate. Resolution splits by kind: a module-qualified function already evaluates to a callable through ordinary member access (`var f = M.E` has always worked), so it needed only that same resolution; a **static method is not a value** — reading `C.Method` deliberately raises "call it with parentheses" — so it is wrapped in a new `ToshStaticMethodReference`. **Order matters between the two and is commented**: letting the value path run first threw that error before the wrapper branch was ever reached, which is exactly what happened on the first attempt. The reference stands for the whole overload set rather than one signature — arity is settled at call time by the ordinary dispatcher, since a second resolver in the reference is the `TS-P1-24` shape — and an overloaded static is asserted through both arities. | **Remaining: `&$obj.Method`**, a reference bound to a receiver. It is a different feature from referencing a named function — it needs a value that carries both the instance and the method, and parser support for `&` before a variable — so it is left rather than half-built. Covered by `InvocationSurfaceTests`, which will fail the day it is added if the rest regresses. | |
| `TS-P2-101` | Complete for declarations — fixed 2026-08-14; member-level help remains, see intent | **Class and member doc comments never reach `help`.** A `##` block on a class parses into `ClassDefinitionStatementSyntax.DocComment` and is indexed for LSP hover, but `ToshClassDefinition` has no `DocComment` field, so nothing carries it to runtime. Measured: `help Color` on a fully documented class returns only the synthesised `ToSh type Color.`, while `help add` on a documented function returns description, per-argument text and examples. The same gap covers class properties and methods, interfaces, enums, records, modules and type aliases; `trait` is worse still, being absent from the LSP declaration index entirely. A library documented to the house standard is therefore discoverable in the editor and invisible in the shell. | `help <Type>` and `help <Type>.<member>` return the parsed doc comment for classes, their properties and their methods, matching what functions already return. Traits appear in the LSP declaration index. A corpus asserts on summary, remarks and example text for a documented class, one of its properties, one of its methods, and a documented trait. **The parse side was already complete** — `ClassDefinitionStatementSyntax.DocComment`, `ClassPropertyMemberSyntax.DocComment`, and a method's wrapped `FunctionDefinitionStatementSyntax` all carry it — so nothing needed parsing work; the comment simply stopped at the runtime definition. `IShellTypeDescriptor` gains a **defaulted** `ShellDocumentation` (null), so a CLR-backed descriptor is not made to declare it has no ToastScript comment, and the five ToastScript definitions — class, record, struct, enum, interface, plus trait — store and surface theirs. `CreateShellTypeTopic` prefers the author's summary over the synthesised line. **All five kinds were done together on purpose**: each dropped the comment at the same place, so repairing one would have left the surface inconsistent in a way a single-kind test could not see. Three controls: an undocumented type still gets the synthesised description (an empty help entry is worse than a generic one), only `@summary` is promoted rather than the whole block, and documented *functions* — the reference behaviour — still work. The test probe had to be corrected on the way: `help` yields a topic object the CLI renders, so capturing stdout measured the renderer and did so inconsistently — a class printed while a struct produced nothing, which read as a missing fix rather than a mis-aimed probe. 8 tests; reverting only the catalog fails 6. | **Remaining: `help <Type>.<member>`.** It does not resolve today for any type — `help K.P` answers "was not found" — so property and method documentation needs a help *resolution path that does not exist*, which is a feature rather than the plumbing this item turned out to be. Interfaces, enums, records and modules are done; `trait`'s absence from the LSP declaration index is also still open. | |
| `TS-P2-104` | Complete for calls — fixed 2026-08-14; two parser gaps remain, see intent | **A splat argument is rejected wherever it would be useful.** `f(...$values)` reports `tosh.runtime.expression_failed: Unsupported argument syntax: SplatArgumentSyntax`. Measured while passing a list of flags to a variadic helper: `Bits.Or(...$flags)` fails, so a caller holding a list has no way to spread it into a variadic parameter and the callee has to flatten defensively instead. The syntax parses — it is the evaluator that has no case for it — so the failure arrives at runtime rather than at bind time. | `f(...$list)` spreads the list into the callee's parameters, for both variadic and fixed-arity targets. If spreading is not going to be implemented, the parser rejects the syntax so the diagnostic arrives before the script runs. A corpus covers a variadic callee, a fixed-arity callee, an empty list, and a splat combined with ordinary leading arguments. **Narrower than filed, and measuring said so.** A **bare-name** call spread correctly all along; every *dotted* call failed — instance, static, module-qualified and CLR. The cause was **two argument evaluators, only one of which knew the language had spreading**: the bare-name path used the splat-aware one, while constructors, method calls and qualified calls used a second walker producing one value per syntax node. They are now one implementation, so a future argument form cannot reach half the language again. The control took two attempts and the first was wrong in an instructive way: a list handed to a **rest** parameter already spreads without any splat — `CountArgs($f)` counted 3, before this change as well as after — so a variadic callee cannot distinguish splatting from ordinary passing and proves nothing. A **fixed-arity** callee can, refusing the bare list and accepting the splatted one, and that pre-existing rest behaviour is now pinned so it is not mistaken for this item later. | **Remaining, both in the parser rather than the evaluator.** `new P(...$pair)` does not recognise `...` at all — the constructor receives the literal string `"...$pair"` as one bareword argument — and `f(...[])` with an array *literal* fails to parse with "Arguments must be separated by ','". The realistic spelling `...$empty` works. Neither is in this item's measured examples or its acceptance corpus; filed here so the parser half is not mistaken for done. | |
| `TS-P2-117` | Open | **A line beginning with a unary operator is absorbed into the previous line's expression.** `var r = true` followed on the next line by `not $r` reports `Variable 'r' was not found` at 2:5 — the binding never happened, because the two lines parsed as one expression continuing across the break. Measured on `HEAD` (`6351da8`) in a clean worktree, so it is older than `TS-P2-116` and independent of it: `var r = 5` then `not true` fails at **1:9**, the *first* line's right-hand side, and `var r = 5` then `+ 5` produces nothing at all. The boundary is strange and is the reason this is filed rather than folded in: a leading *binary* line is fine (`var r = 5` then `6 + 3` answers 9, and `var a = 1` / `var b = 2` / `$a + $b` answers 3), and the same two statements separated by `;` on one line are also fine (`var r = true; not $r` answers `False`). It appears to depend on what the previous right-hand side was — `var m = 12` then `bnot $m` answers -13, while `var r = true` then `not $r` fails. | Decide first whether a leading operator is *meant* to continue the previous line, because that is a language question and not a parser bug: if it is, the ambiguity with a statement that opens with `not`/`-`/`+` needs a stated rule; if it is not, the continuation should stop at the line break unless the line ends with an explicit `\`. Either way the two-line form must stop silently discarding the binding. A corpus should cover both readings of `var r = true` / `not $r`, the `;` form that already works, the binary-leading lines that already work, and whatever explicit continuation the rule settles on. |
| `TS-P2-118` | Open | **Calling a callable held in a property warns `member_not_found`, then works.** `$c.Handler(7)` where `Handler` holds a function reference reports `⚠ tosh.type.member_not_found — Method 'Handler' was not found on type 'C'` and then returns 107. The runtime is right and the type checker is wrong, which is the worst pairing: the code works, so the warning is noise, and noise is what teaches people to stop reading warnings — the same reasoning as `TS-P2-09`. The `$this.Handler(...)` form inside a method does *not* warn, because `TS-P2-93` taught that path about callable properties; external access through an instance was not covered. Found while auditing the user's `Gl.tosh` and `Gtk.tosh`, whose remarks still described the whole shape as impossible. | Teach the member-resolution check that a property whose value is callable is invocable, in whatever the shared place is — the fix should not be a second copy of the `TS-P2-93` rule, since the point is that one rule now lives in two paths. A corpus should cover the instance form, the `$this` form that already works, a property holding a lambda as well as a `&` reference, and a control that a genuinely missing member still warns. |
| `TS-P2-120` | Open | **A nullable annotation on a built-in type alias will not parse in a `var` declaration.** `var x: int? = null` and `var x: string? = null` both fail with `tosh.bind.unknown_command` — *"Command 'var' is not a registered builtin or function declared in this source"* — a message that never mentions the annotation and points at column 1. The boundary is specific and is what makes it look like a parser special-case rather than a missing feature: `var x: Nonexistent? = null` parses fine and reaches the runtime annotation check, and `func f(a: int?)` works in parameter position. So the form is understood everywhere except a `var` declaration naming a built-in alias. Found while writing the `TS-P2-119` corpus; reproduces identically on a build of `HEAD`, so it is not related to that work. | Find where a `var` declaration's annotation is parsed and why a known type alias followed by `?` diverges from an unknown name followed by `?` — the likely cause is the `?` being taken as the ternary operator once the preceding token resolves as a type. A corpus should cover `var` and `const` declarations for a built-in alias, a user class, an unknown name, and the parameter and return positions that already work, plus the diagnostic naming the annotation rather than the statement keyword. |
| `TS-P2-125` | In progress — steps 1 and 2 of 3 done 2026-08-16 | **A lexical scope allocated ten collections it almost never used.** Measured with `GC.GetTotalAllocatedBytes`, an **empty** `for` iteration allocated **2,797 bytes** — and a scope is pushed per block, so per iteration. `LexicalScope`'s constructor eagerly built eight dictionaries and two hash sets — Commands, Classes, Modules, RefinementTypes, TypeImports, TypeAliases, NativeTypes, LocalEventNames, HushedCodes — of which a loop body uses none. | **The tables are created when something is first put in one.** Every call site is unchanged, because `scope.Commands[name] = command` still reads the same; the cost is that a *read* of an empty table materialises it, which is acceptable since the table read on the hot path — variables — was always going to exist. **Measured: 2,797 to 2,187 bytes per iteration, and 1.50 to 1.16 µs**, a consistent 608 bytes saved per scope push across every shape measured. **What the measurement changed.** The working theory was "async-per-node": that the cost is async state machines. The numbers say otherwise — a bare `$s = $t` costs only 373 bytes over an empty iteration, while *parentheses alone* cost 5,472 (`$s = $t` 2,560 → `$s = ($t)` 8,032) and each further operator about 5,824. So the expense is concentrated in expression evaluation, not in the async machinery as such, and the remaining plan (`ValueTask`, then a synchronous fast path) should be re-aimed at that before it is written. **A caution about the profiler.** A `dotnet-trace` allocation profile named `ToshLexer.TryCreateIntegerToken` as 43% of allocations — code added the same day for `TS-P2-123`, which rebuilt a four-element suffix table on every call. Hoisting it to a static changed the per-iteration numbers *not at all*: the cost was parse-time, and the trace's sampling had misattributed it against a loop allocating gigabytes. The fix is kept because it is right — that table is rebuilt for every numeric-looking token in every file parsed — but it is not what the trace claimed. **Step 2: the cost was found by counting calls, not by sampling.** Temporary counters on each path answered what the profiler had not: a bare `$s = $t` never enters `EvaluateArgumentAsync` at all, `$s = ($t)` enters it twice per iteration, and `$s = ($t + 1)` three times — at roughly **2,545 bytes per entry**. That is far more than a `Task` (~80 B): it is the state-machine box of a ~1,000-line `async` method carrying locals from all 39 of its branches. Every expression node paid for the largest branch. **The fix is a synchronous pre-dispatch** in front of it — a non-`async` wrapper returning `ValueTask<object?>` that answers the shapes which cannot suspend (a literal, a resolvable variable, and parentheses around either) without entering the async method at all, which is steps 1 and 2 of the plan arriving together. **`$s = ($t)` fell from 8,034 to 2,825 bytes (-65%) and `$s = ($t + 1)` from 11,146 to 6,057 (-46%)**; end to end the million-iteration arithmetic loop went 1.455s to 1.272s and the annotated one 1.645s to 1.487s. The variable case is guarded to match the full one exactly — it falls through when the binding is declared-but-unassigned, holds a rune thunk, or is an out-of-scope constructor parameter — because answering those here would skip their diagnostics. The first draft used a plain value lookup and would have done precisely that; the guard was written after reading the case it replaces rather than after a test failed. **Widened to arithmetic, 2026-08-16.** Pure arithmetic over primitive numbers joins the synchronous path, and because the check recurses, a nested expression collapses entirely: `($t + 1 + 2 + 3)` now enters the async method zero times instead of three. **A/B against the previous commit: -15.1% on `$s = ($t + 1)` and -25.0% on the four-operator form**, the gain scaling with operator count exactly as it should when each avoided entry saves ~2,545 bytes. Comparison, membership, regex and string concatenation are deliberately excluded — each awaits — and either operand being a class instance means a user-defined overload that awaits too. Constant-folded operators are answered from the annotation the lowering pass already stamped. **Two measurement lessons.** A first reading showed the annotated loop 8% *slower*; an A/B against a worktree build of the previous commit put it at +0.5%, i.e. noise — the same trap as `TS-P2-122`, and the second time this session that a cross-run comparison nearly caused a wrong conclusion. And the two standing benchmarks showed no change at all for a good reason: they are built from `$x += 1` and `$x == N`, a compound assignment and a comparison, neither of which the fast path covers. A benchmark that does not exercise the change reports honestly that nothing happened. **The operator itself, 2026-08-16.** `EvaluateBinaryOperatorAsync` is now a non-async wrapper over its async core, taking the synchronous route when the operator is pure arithmetic and both operands are primitive numbers. Placed there rather than at the call sites so every caller benefits at once — a compound assignment, a chained comparison and an operator argument all arrive through it. **-3.2% on the `$x += 1` loop and -2.2% on the annotated one**, with the expression benchmarks unchanged because the argument-level path already covered them. **Two queued items were declined, and the reason is `TS-P1-24`.** A fast path for *comparison* would be a third implementation of equality: `AreEqualAsync` and `OperatorEvaluator.AreEqual` are the parallel pair that item was filed for, its own comment records that an earlier fix landed on only one of them, and the tail awaits `TryConvertForEqualityAsync` for both-direction coercion on top of enum equivalence, records-by-name and collection recursion. A fast path for *member access* has the same shape: `ReflectionObjectAccessor` already carries `GetValue` beside `GetValueAsync`, and routing through the synchronous twin would silently skip whatever the async one does for a computed property. The binary-operator shortcut was safe precisely because it is *not* a copy: it calls the same `OperatorEvaluator.EvaluateBinary` the async path reaches. That is the test worth applying to the remaining shapes — a shortcut to the existing implementation is safe, a second implementation of the same rule is the defect this project keeps finding. |
| `TS-P2-11` | Corpus complete 2026-08-09; Pratt rewrite **not recommended** — see intent | Parser expression layers rely on scattered lookahead and special cases. | Adopt an explicit precedence/postfix architecture, preferably Pratt-style, without changing accepted syntax unintentionally. **Measured, and the premise does not hold.** The expression layers are *already* an explicit precedence cascade: nine levels from `ParseTernaryExpression` down to `ParseUnaryExpression`, conventional and readable, with correct associativity throughout (`**` right, `-` and `/` left). Flattening them into a Pratt table would be a stylistic change across a 13,000-line parser carrying real regression risk and nothing user-visible. The scatter the row describes is real but sits at **statement** dispatch — roughly 30 `LooksLike*` predicates, 106 lookahead helpers in total — which a Pratt expression parser does not touch. Recommending against the rewrite as written; if the lookahead scatter is worth attacking it should be filed against statement dispatch, where it actually lives. **The corpus this row's own status line started is the half worth having, and it is done** — 25 execution-pinned cases covering arithmetic precedence and associativity, comparison, `and`/`or`/`not`, null-coalescing, the ternary, postfix member access and indexing, and grouping. It states the binding rules as facts, which is what would make any later restructuring safe. **Writing it immediately found two defects a refactor would have preserved without noticing**, both filed: `TS-P2-76` — `..` binds tighter than arithmetic, so `1 .. \$n + 1` fails with a message about `Int32` and `ToshRange` rather than about grouping — and `TS-P2-77` — a parenthesised left operand ends a `for` iteration source early, so `for i in (1) .. 3` reports `expected_block`. Both are pinned in the corpus as current behaviour, marked as defects and pointing at their items, so a reader can tell a rule from a bug. Full suite 4,496 passing. |

## P3 — Features Enabled by Stabilization

| ID | Status | Problem | Required acceptance |
|---|---|---|---|
| `TS-P3-04` | Research | Explicit stream/collection shape | Remove cardinality lookahead while preserving object-valued pipelines and a reasonable migration path. (Concrete motivating asymmetry: `[1,2,3] \| count` is 3 while a piped dictionary counts as 1.) |
| `TS-P3-07` | In progress — Stages A–C implemented 2026-08-12; focused validation paused | Unify `StorageSize`/`TemporalAmount` with the Quantity unit system | The source of truth is [`UNIT_SYSTEM_STABILIZATION.md`](UNIT_SYSTEM_STABILIZATION.md). Implemented: compound transforms and canonical derived arithmetic; ``quantity as `unit`` plus `.To(...)`; `°`/`°C`/`°F`/`°R`; typed script strings and friendly quantity annotations; compiler literal/boundary support; aggregation; duration/data bridges; deterministic reactor dogfooding; prefix policy; unit-aware `sleep`; immutable dimension views; synchronized non-mutating registry reads; generated diagnostics and TextMate grammar. `DataSize` is the physical category while `StorageSize` remains the exact integral-byte bridge; `DurationQuantity` is fixed time while `TemporalAmount` remains calendar-aware. Remaining Stage D work is explicit temperature-difference and semantic-kind metadata, engine-scoped custom-unit overlays, resolved-unit serialization for compiled assemblies, editor recovery, and later operator extensions. Built and run 2026-08-12: full suite **4,692 passing**, zero failures. Three failures found at that point were fixed — the `pure` compile profile had started referencing `Tosh.Compiler.Runtime` through the new `ToshHost.CheckType` conversion boundary (gated to the corelib path for that profile, as `TS-P1-25` requires), `examples/reactor-block.tosh` ignored its own declared `ReactorType` arg and always prompted through shell-only `tui`, and the reactor test compared `int` literals against `double` properties. Command metadata, PDFs, and VSIX regeneration remain pending. Storage suffix migration still needs a compatibility plan because historic case-insensitive `kb` means kilobytes while physical notation uses `kb` for kilobits. |
| `TS-P3-12` | In progress — REPL half fixed 2026-08-01; **editor word registry and grammar structure done 2026-08-15**; require-following was already complete; vsix reinstall still pending | **Type highlighting misses real contexts, differently per surface.** Reported from a profile.tosh screenshot: `$this` renders harsh red in VS Code, `<T>` and declared classes uncoloured, `→ FileSystemEntry` return annotations plain. Three separate renderers are involved and each had different gaps. **REPL (fixed):** the type-context heuristic knew `->` but not `→` — and the Unicode arrow is what real profiles contain — and never treated `is`/`is not`/`as`/`where`/glued-`<` as type positions, so match arms and generic arguments went uncoloured; `SyntaxHighlighterTests` covers each context with a negative control. **VS Code (partly config, partly pending):** `$this` was scoped `variable.language.this`, which most themes render bright red — rescoped to `variable.other.member.this.tosh`, the member family the reporter asked for — and the family the grammar already uses for `variable.other.member.tosh`, which `variable.other.property` was not. **Corrected 2026-08-08:** the rescope was first written into `syntaxes/tosh.tmLanguage.json`, which is *generated* by `scripts/generate_vscode_grammar.py`, so the next regeneration silently reverted it and a reinstall showed no change. It now lives in the generator at the rule for `\$this`. Diffing a fresh regeneration against the working tree confirms the JSON is entirely generator output, so nothing hand-written was lost with it. The LSP already computes semantic tokens for *declared* type names (`GetVisibleTypeLikeSymbols`) but the extension contributed no `semanticTokenScopes`, so themes without direct semantic colours dropped them — mapping added. Both edits sit in the working tree alongside ~800 lines of the user's own uncommitted editor work, so they are **not committed here**; they ship with the user's next extension commit and need a vsix rebuild+reinstall to see. The screenshots may partly reflect the previously installed vsix, so `<T>` should be re-checked after reinstall before touching the grammar further — the working-tree grammar has rules that look correct for it. **Grammar audit, 2026-08-01, driven by tokenizing real code rather than reading rules.** A `vscode-textmate` harness over the users own profile and libraries measured **8,916 tokens, 2.3% unscoped**, and the clusters named the defects. **Fixed:** (1) *record and dict keys took whatever scope their spelling implied* — in one literal, `local` scoped as a storage modifier, `config` as a builtin command and `data` as a function call: three colours for three things of one kind. Records and dicts now have real begin/end contexts with a key rule, and that rule starts at the whitespace *before* the name so it beats the command rules, which is the tie-breaking trick `command-position` already documents for itself. (2) *`out`/`ref` scoped as control flow* though they are parameter directions — now `storage.modifier.direction`, so they read like `shy` and `raw` rather than like `if`. (3) *native marshalling types* (`cstring`, `cstr`, `buffer`, `handle`) fell through to the generic type rule — now builtins. (4) *success contracts* `-> ok` and `-> count` rendered as ordinary types, losing the distinction that makes a native signature readable; they now have `support.type.contract`, kept under `support.type` so themes that know only that still colour them. (5) *optional parameter markers* left `path?` entirely unscoped, because the name class excludes `?` and the rule demanded an immediate colon. \| **Remaining, and it is structural rather than a missing rule:** untyped parameters — `func +(o)`, `func f(other)` — stay unscoped, 38 occurrences in the users own code. The parameter rule requires a following colon, which is what stops it mis-scoping a command call inside parentheses: from a regex alone, `(o)` in a signature and `(greet)` in an expression are the same shape. The fix is a begin/end context for function signatures, `func <name>(` … `)`, inside which every bareword is a parameter — the same structural move that fixed record keys. Worth doing deliberately rather than as a regex widening, because signatures appear in class bodies, native binds, operator declarations and lambdas, and a bad block boundary would mis-scope all of them. **Grammar structure, 2026-08-15, measured not read.** The `vscode-textmate` harness was rebuilt and run over the user's libraries, the examples and their profile: **31,333 tokens, 2.22% unscoped**. Two entries this row already recorded as *fixed* — record and dict keys, and the `path?` optional marker — were **measurably still broken**, and the reason is the trap this row documents about itself: they were written into the generated `tosh.tmLanguage.json` and a later regeneration dropped them. The generator contained neither rule. All four gaps are now fixed **in the generator**: (1) *signature parameters* — the declaration rules open a region at their `(` and close it at the matching `)`, with a `nested-parens` rule so a parameter whose own type is a function (`cb: func(int) -> int`) cannot end the signature at its inner paren; this is what lets an untyped `func f(other)` be told from a call `greet(name)`, which no regex can do, and it subsumes `path?` by making the marker part of the name. (2) *record, dict and set literals* are regions with a key rule, so a key can be told from a defaulted parameter `func f(a, b = 2)` — the reason a `match` rule was not enough. (3) *type-alias right-hand sides* — `type IntPercent = int where …` left the base type unscoped in every alias. **Measured after: 528 unscoped, 1.69%**, and the residual `signature-parameter` count is the classifier's own false positives (a `func` line whose body contains `\| where Modified > $now`). Verified by tokenizing that the region closes on every shape — operator overloads, generics, splat parameters, a function-typed parameter, and the paren-less `func f => 1` — and, as the control that matters, that `var x = greet(name)` still leaves `name` unscoped. One deliberate imprecision: inside a function-typed parameter, `func(int)`'s positional `int` scopes as a parameter name, because a bare name there is genuinely ambiguous with an untyped parameter; it is coloured rather than invisible, which is the better of the two. **Guarded** by `EditorSurfaceParityTests`: the structural rules must exist in the checked-in artefact, and the parameter rule must not have regained its `(?=:` requirement — the specific regression that made it useless. Both were confirmed to fail when reverted. | Remaining: the `tosh.lang` GtkSourceView grammar has none of the four structural fixes — it is a separate hand-written file and only the VS Code grammar was measured. Re-check `<T>` and declared-class colouring after the vsix reinstall. The require-following work described here as outstanding was already complete, with `RequiredTypeVisibilityTests` covering the chain, the cycle, the depth bound and the URI form; the description below predates it. |
